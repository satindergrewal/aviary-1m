# M7 decision probe -- and it found something much bigger than the prefill gap
# box GPU0, qwen3-4b IQ4_KT, 22,000-token prompt + 256-token decode, -c 40960 -b/-ub 512,
# tip ds4-ports cb3246f9, 2026-08-04 08:0x
#
#            prefill_ms   prefill tok/s   decode ms/tok   decode tok/s
#   static      2,462         8,937           7.78          128.5
#   paged      43,600             -         220.70            4.5
#
# ★★ THE PAGED PATH IS ~18x SLOWER TO PREFILL **AND ~28x SLOWER TO DECODE** AT 22K CONTEXT.
# 4.5 tok/s for a 4B model on a Blackwell card is absurd on its face (accepted-absurdity
# rule): every prior paged measurement in this lane was at <=8K context with small pools,
# where the constant factors hide. The kernel's shape is the cause and it hits BOTH phases:
#   - one CUDA block per (head, seq, query token), head_dim (=128) threads per block;
#   - each block walks the ENTIRE visible context serially, one KV token at a time;
#   - so a decode step at 22K context = 22K serial iterations on 128 threads, with no
#     KV-axis parallelism (no split-K / flash-decoding) and no data reuse.
# Static banded FA parallelises over the KV axis and reuses tiles, hence 28x.
#
# ⚠ OPERATIONAL CONSEQUENCE (record it before anyone serves on this): --kv-paged is
# correct at long context but NOT fast -- it is a scheduling/memory win today, not a
# throughput win. Do not put a long-context production serve on the paged path until the
# kernel work below lands. Everything P2-8 proved (admission, preemption, sharing) STANDS;
# this is the kernel underneath it.
#
# ★ M7 SCOPE CORRECTED: it is not "tiled prefill", it is "the paged attention kernel needs
# KV-axis parallelism + tiling in BOTH phases":
#   1. decode: split the context across warps/blocks (flash-decoding), combine partials by
#      log-sum-exp -- the same shape the current kernel already uses ACROSS warps, extended
#      to a grid split so long contexts get real parallelism.
#   2. prefill: two-level (Q_TILE x KV_TILE) tiling per DESIGN-M7-TILED-PAGED-PREFILL.md.
#   3. or option B from that doc (gather + delegate to the already-gated banded FA kernel),
#      which now looks stronger: it fixes BOTH phases at once by reusing a kernel that is
#      already fast and already correctness-gated on both backends.
# Decision rule from the design doc is ANSWERED: prefill share is 55% (static) / 44%
# (paged) at this working point -- prefill matters, and decode matters just as much.

# == M7 PHASE 1 SHIPPED + MEASURED: split-K decode (ds4-ports 483ffb05) ==
# Same 22K probe, same box/card, after the split-K decode kernel + log-sum-exp combine:
#            decode ms/tok      decode tok/s     prefill_ms
#   before      220.70              4.5            43,600
#   after        36.18             27.6            43,726   (prefill untouched by design)
#   static        7.46            134.0             2,412
# ★ 6.1x DECODE SPEEDUP, equivalence suites green (test-paged-kv + test-paged-banded ALL
# PASSED with split-K auto-enabled at this context length). Still 4.6x off static, which
# is the expected residual: the slices are merged correctly but each slice still walks its
# span one KV token at a time with no tile reuse -- tiling is the next increment, and it
# lifts prefill (untouched here, 82.5% of this request now) at the same time.
# Auto-enable threshold: pool span >= 4096 tokens. DS4P_PAGED_SPLITK=N forces, =0 disables
# (=0 keeps the whole-context path bit-for-bit).

# == M7 PHASE 2 (tiled prefill, ds4-ports 4cb656dd, DS4P_PAGED_QTILE=1): 1.40x -- GATE NOT MET ==
#   prefill_ms 43,726 -> 31,272   (static reference 2,488)
#   test-paged-kv ALL PASSED; test-paged-banded ALL PASSED **with QTILE=1** (equivalence holds)
#   gate wanted >=5x. Flag STAYS OFF by default.
# ★ WHY (root, from the kernel I wrote): Q_TILE=8 cuts KV *loads* 8x but my mapping does
#   EIGHT separate warp-shuffle reductions per KV token (one per query in the tile) where
#   the old kernel did one. Loads went down 8x, reduction work went UP 8x -- net 1.4x means
#   loads were only part of the cost and the per-pair reduction is now the bottleneck.
# ★ THE FIX (next increment, this is what real FA does): stop reducing per (query, key)
#   pair. Map lanes to a Q_TILE x KV_TILE SCORE BLOCK -- each lane owns one (query, key)
#   product chain over a dim slice, so a warp computes 32 scores at once instead of one;
#   accumulate V into per-query registers from the score block. That is the MMA-shaped
#   inner loop; the outer structure (tiles, band, online softmax, cross-warp merge) is
#   already written and equivalence-gated, so the change is local to the score loop.
# Honest position: phase 2 as written is a correct but modestly-profitable step; it stays
# dark until the score-block mapping lands and the >=5x gate is actually met.

# == M7 PHASE 2b (score block) -- REGRESSED 3.4x, REVERTED (ds4-ports 2f9c4c81) ==
#   22K prefill: 31,272 ms (phase 2 tiled) -> 106,201 ms (score block) = 0.41x
#   test-paged-banded still ALL PASSED -> the MATH was right, the MEMORY SHAPE was wrong.
# ★ WHY: "lane owns a whole query's dot product" removes the shuffles but destroys
#   COALESCING -- each active lane then streams all head_dim values of the SAME k vector
#   from global memory by itself, so the warp re-reads one cache line q_cnt times instead
#   of loading it cooperatively (32 lanes x 4 dims) once.
# ★ LESSON (worth more than the commit): shuffles were not the whole cost. A mapping that
#   deletes them by breaking coalescing loses more than it saves. The win must keep
#   lane-owns-dims cooperative loads AND amortize each load across queries -- i.e. phase 2's
#   shape (4cb656dd, 1.40x) is the right skeleton and needs a cheaper per-query reduction
#   inside it (e.g. score several queries per shuffle tree, or stage K in smem once per
#   tile so the reduction reads smem instead of re-reading global).
# STATE: reverted to phase 2's mapping; DS4P_PAGED_QTILE still dark; M7 prefill OPEN with
# the search space now measured from both sides (too few reductions AND too little
# coalescing both lose).

# == M7 PREFILL: THREE MAPPINGS MEASURED, SHAPE'S CEILING FOUND (final, tip 52a58345) ==
#   22K prefill_ms      mapping
#   43,726              baseline (one block per query, no tiling)
#   31,272   1.40x      phase 2: Q_TILE=8, lane-owns-dims loads + per-query shuffle reduce
#  106,201   0.41x      phase 2b: lane-owns-a-whole-query (no shuffles) -- COALESCING LOST
#  218,100   0.20x      Q_TILE=16 -- REGISTER SPILL (16 queries x 4 acc + m/l per lane)
#   (static banded-FA reference: 2,488-2,542 ms; the >=5x gate needs <= ~8,745 ms)
# ★ CONCLUSION, measured from three directions: this kernel family's binding constraint is
#   REGISTER PRESSURE, not KV traffic. Q_TILE=8 is its local optimum at 1.40x, and the
#   >=5x gate is NOT reachable by tuning this shape -- bigger tiles spill, and removing the
#   reductions costs more coalescing than it saves.
# ★ THE ROUTE THAT REMAINS: an MMA / tensor-core inner loop -- score tiles held in warp
#   fragments instead of per-lane accumulator arrays, which is exactly how flash-attention
#   escapes this ceiling (and why the static banded kernel is 12-18x faster here). That is
#   a different class of work: new kernel, fragment layouts, a correctness gate of its own.
#   Until it lands, DS4P_PAGED_QTILE stays DARK and paged prefill keeps the baseline path.

# == FULL Q_TILE SWEEP -- the optimum is 4, not 8 (tip 3aca564f) ==
#   Q_TILE  2 : 31,277 ms        Q_TILE  8 : 31,272 ms
#   Q_TILE  4 : 28,816 ms ★      Q_TILE 16 : 218,100 ms (spill)
#   baseline 43,726 · static reference ~2,500 · gate needs <= 8,745
# ★ 1.52x at Q_TILE=4 -- a real INTERIOR optimum: below 4 the coalesced K load stops
#   amortising, above 4 accumulator pressure costs more than the reuse buys.
# ⚠ CORRECTION TO THE PREVIOUS ENTRY: I called Q_TILE=8 "this family's local optimum"
#   after bounding the curve only from ABOVE (16 spilled). Measuring the LOWER side moved
#   it. The finding that survives is the CEILING (~1.5x, gate needs 5x), not the location
#   of the peak -- and the lesson is that a curve bounded on one side is not bounded.
# Conclusion unchanged in substance: this shape tops out ~1.5x, the >=5x gate needs the
# MMA/fragment inner loop, and DS4P_PAGED_QTILE stays dark meanwhile.

# == DARK-DEFAULT REGRESSION CHECK (tip 3aca564f, DS4P_PAGED_QTILE unset) ==
#   prefill 43,717 ms == the 43,726 ms pre-M7 baseline (0.02% apart, run-to-run noise)
#   decode 36.4 ms/tok == split-K's 36.2 (split-K IS on by default at this ctx, as designed)
# ★ Proof the flag is genuinely dark: after five kernel edits (tiled kernel, score block,
#   revert, three tile sizes) an unflagged serve gets exactly the pre-M7 prefill path and
#   the shipped split-K decode. Nothing a user runs was ever exposed to the dark work --
#   which is the point of shipping it dark, now measured instead of asserted.

# == M7 STAGE 3b: WMMA FRAGMENT PREFILL, WIRED AND MEASURED (ds4-ports 07113d21) ==
#   22K prefill: 87,793 ms  (baseline 43,726 · best f32 tile 28,816 · static ~2,500)
#   = 3.0x SLOWER than the f32 tile path. Gate (<= 8,745) not approached.
#   test-paged-banded ALL PASSED under DS4P_PAGED_QTILE=2 -> the FRAGMENT MATH IS RIGHT.
# ★ ROOT: the shape, not the tensor cores. One warp owns a 16-query tile (32 threads per
#   block), so the SM sits nearly empty, and the per-row online softmax -- 16 rows x
#   head_dim scalar rescales per key tile -- serialises across that single warp. The
#   matmuls are fast; everything around them is 32 threads wide.
# ★ NEXT SHAPE (not a new idea, the same kernel with more warps): multiple warps per block
#   -- one per key tile or per row group -- so staging, softmax and the fragment ops
#   overlap instead of queueing behind 32 threads. That is the difference between "uses
#   tensor cores" and "is a flash-attention kernel".
# STATE: DS4P_PAGED_QTILE unset/=1 untouched and still the measured paths; =2 is the dark
# fragment path. Three shapes now measured for prefill (f32 tile 1.52x, score-block 0.41x,
# fragment 0.50x) -- all dark, none shipped, the baseline path is what anyone runs.

# == MULTI-WARP FRAGMENT PREFILL (ds4-ports 52b036ab): 87,793 -> 31,915 ms (2.75x) ==
#   test-paged-banded ALL PASSED under QTILE=2. Now at parity with the best f32 tile
#   (28,816) and still 3.6x off the <= 8,745 gate.
#   Fix: block is 4 warps -- staging, per-row softmax rescale and writeback are block-wide;
#   fragment ops stay warp-0 (wmma is warp-scoped); barriers upgraded to __syncthreads.
#   This confirms the s3b diagnosis exactly: the 32-thread shape, not the tensor cores.
# ★ WHAT REMAINS IN THIS SHAPE: warp 0 issues both matmuls while three warps idle through
#   them. Real flash-attention gives each warp its OWN key tile and merges the per-warp
#   (m, l, acc) states at the end -- a structural change, not a knob. That is the next
#   commit, and it is the last idea in this line that has a mechanism behind it.
# PREFILL SCOREBOARD (all dark, baseline is what runs): baseline 43,726 · f32 tile 28,816
#   (1.52x) · score-block 106,201 · fragment 1-warp 87,793 · fragment 4-warp 31,915.

# == PER-WARP KEY TILES (ds4-ports 7171665a): 53,287 ms -- parallelism won, occupancy lost ==
#   Each warp owns its own key-tile stream + softmax state, log-sum-exp merge (the split-K
#   pattern applied to prefill). test-paged-banded ALL PASSED -- partition math right.
#   SLOWER than the shared-accumulator 4-warp (31,915) because four private o_acc f32
#   regions push smem to ~80 KB/block -> ~1 block/SM where the shared shape ran several.
# ★ THE MECHANISM LINE IS NOW COMPLETE WITH DATA ON EVERY BRANCH:
#   baseline 43,726 · f32 tile Q4 28,816 (best) · f32 Q8 31,272 · f32 Q16 218,100 (spill)
#   score-block 106,201 (coalescing) · frag 1-warp 87,793 (occupancy) · frag 4-warp shared
#   31,915 (warp0-only mma) · frag per-warp 53,287 (smem/occupancy).
#   Gate <= 8,745. Static ~2,500. Remaining distance = PIPELINING ECONOMICS (cp.async
#   double-buffering, fragment reuse across tiles = the fattn-mma-f16.cuh discipline), not
#   another partition of the same work. That is a port-class effort with its own gate.
# All shapes dark. Baseline is what runs, and that is measured (43,717 unflagged).

# == DOUBLE-BUFFERED PIPELINE (ds4-ports 18953b60): 32,559 ms -- FLAT, hypothesis killed ==
#   Warps 1-3 prefetch T+1 while warp 0 computes T; smem +8.7 KB only (occupancy held);
#   test-paged-banded ALL PASSED. Overlap bought NOTHING vs 31,915 -> staging latency was
#   NOT the cost. The per-tile floor is warp 0's serial compute (softmax rows + fragment
#   issue); staging was already hidden by 4-warp parallel loads in the shared shape.
# ★★ NINE SHAPES, EVERY BRANCH PRICED, INCLUDING PIPELINING:
#   baseline 43,726 · f32 Q2 31,277 · f32 Q4 28,816 BEST (1.52x) · f32 Q8 31,272 ·
#   f32 Q16 218,100 (spill) · score-block 106,201 (coalescing) · frag-1w 87,793
#   (occupancy) · frag-4w-shared 31,915 (serial warp0) · frag-per-warp 53,287
#   (smem/occupancy) · frag-double-buffer 32,559 (staging was not the cost).
#   Gate <= 8,745. Static ~2,495. The remaining 3.7x REQUIRES fragment-held state across
#   tiles with multiple mma warps -- the fattn-mma-f16.cuh architecture -- a port with its
#   own gate, now justified by elimination rather than assumption.
# All dark. Baseline measured-untouched (43,717 unflagged).

# == 2-WARP PER-WARP (experiment: 7171665a kernel at dim3(64), smem halved): 58,865 ms ==
#   WORSE than 4-warp per-warp (53,287). test-paged-banded ALL PASSED.
# ⚠ SELF-CORRECTION #2: my occupancy diagnosis for the per-warp shape was WRONG (or at
#   most half the story). Halving the private-accumulator smem should have RECOVERED time
#   if occupancy was the binding cost; instead halving the warps halved the key-tile
#   parallelism and lost MORE. The per-warp shape's real problem is total serial work per
#   warp. Diagnoses need their own discriminating walls -- this one got its wall late.
# TEN WALLS TOTAL. The cluster is unambiguous: every working variant sits in 29-59 s; the
# gate is 8.7 s; static is 2.5 s. The family cannot reach the gate by rearrangement --
# now shown by exhaustion across tile size, mapping, partition, pipelining, AND the
# occupancy counter-test. fattn-mma-f16 architecture (fragment-held state, multi-mma-warp)
# remains the only unpriced branch, and it is a port.

# ============================================================================
# == THE WALL BREAKS: FRAGMENT-HELD mma PREFILL (ds4-ports 76fc9d4a) ==
# ============================================================================
# WALL: 12,615 ms  =  3.47x over baseline 43,726  =  2.28x over the ten-wall best
#       (f32-Q4 28,816) and 2.50x over the best wmma shape (31,525).
# BAR:  test-paged-banded ALL PASSED, max_abs 4.067e-05.
# Static ceiling 2,496. Gate <= 8,745 -- 1.44x still open.
#
# Ported discipline: ggml/src/ggml-cuda/fattn-mma-f16.cuh (upstream llama.cpp,
# Johannes Gaessler). Credited in the kernel header and the commit message.
#
# WHY EVERY EARLIER SHAPE WAS CAPPED (the honest re-reading of ten walls):
#   wmma::fragment is OPAQUE -- there is no defined element mapping, so the
#   accumulator must be stored to shared memory and reloaded once per key tile,
#   and the softmax probabilities must round-trip through shared memory to
#   become the next operand. Six wmma shapes therefore all sat at 29-32 s: they
#   were not bad arrangements of a good mechanism, they were arrangements of a
#   mechanism that cannot hold state. Rearranging something that spills every
#   tile cannot beat spilling. The elimination map was correct about the
#   mechanism (staging flat, occupancy not binding, warp-0 issue flat) and it
#   was correct that the answer was the fattn-mma architecture -- what it could
#   not do was quantify the prize, which turned out to be 2.5x.
#
# THE LOAD-BEARING FACT (verified index-by-index against mma.cuh BEFORE coding):
#   for m16n8k16, tile<16,16,float> (f32 accumulator) and tile<16,8,half2>
#   (A operand) have the SAME per-thread element mapping, pairwise:
#     acc l=0,1 -> i=tid/4,   j=2(tid%4),+1     == A half2 l=0
#     acc l=2,3 -> i=tid/4+8, same j pair       == A half2 l=1
#     acc l=4,5 -> i=tid/4,   j=8+2(tid%4),+1   == A half2 l=2
#     acc l=6,7 -> i=tid/4+8, same j pair       == A half2 l=3
#   So P enters the next mma as make_half2(p0,p1) straight out of the score
#   registers: no store, no reload, no barrier.
#
# SHAPE: block = 4 warps = 4 INDEPENDENT 16-row q tiles (64 tokens). Warps share
#   the staged K/V tile but own disjoint q rows => NO cross-warp softmax merge.
#   Q fragments load once and stay resident for the whole key loop. o_acc lives
#   in 8 register tiles (64 f32/thread). Online-softmax rescale is a register
#   multiply indexed by (l/2)%2; row max/sum is two __shfl_xor across the four
#   lanes sharing a row. V staged TRANSPOSED so P x V is a plain row.col mma.
#   blockDim is dim3(32,4): mma.cuh indexes fragments by threadIdx.x, so a
#   128-wide block would silently compute garbage rows.
#
# == KV-DEPTH 64 (experiment): 18,364 ms -- FAIL, 1.46x WORSE than KV-16 ==
#   Bar green, so a clean speed loss. DISCRIMINATING, because KV-64 stages the
#   SAME total bytes with 4x fewer barriers and 4x less staging-loop overhead;
#   the only thing that worsened was smem 27.9 KB -> 53.2 KB (blocks/SM halved).
#   If the kernel were barrier- or staging-bound, KV-64 had to win. It lost.
#   => the mma kernel is OCCUPANCY/LATENCY-bound; shared memory is the lever.
#   Reverted to KV=16.
#
# HYPOTHESIS KILLED WITHOUT SPENDING A WALL: "static 2.4 s is unfair because the
#   model is banded and the paged path masks instead of skipping." Checked the
#   serve log and the arch: qwen3-4b-dev has NO visibility window, both arms do
#   the same full-causal work. The gate stands as a legitimate target.

# ============================================================================
# == PROFILE: THE KERNEL WAS STARVED, NOT SLOW (ncu, mma kernel live) ==
# ============================================================================
# Four levers had been priced by then -- KV-64 WORSE, smem overlay FLAT,
# rescale-skip FLAT -- i.e. three consecutive results saying the thing being
# reached for was not the thing that cost. Went to the profiler instead of
# spending a fifth wall on a fifth guess.
#
# ncu (sudo for counter access; --launch-skip 700 so the sampled launches sit
# mid-prefill at ~10K context rather than in the tiny early ubatches):
#   grid                (32,1,8) = 256 blocks on ~188 SMs = 0.7 FULL WAVES
#   achieved occupancy  11.33%  ->  5.44 active warps/SM
#   theoretical occ.    16.67%, LIMITED BY REGISTERS
#                       (block limit: regs 2, smem 5, warps 12, barriers 24)
#   top stall           61.8% L1TEX memory dependency (7.2 of 11.65 cycles)
#   DRAM throughput     0.44%   compute 11.1%   L2 2.2%
#   duration            6.9 ms per launch
#
# THE ARITHMETIC THAT SETTLED IT: 256 blocks x 4 warps = 1024 warps / 188 SMs
# = 5.44 warps per SM. The profiler measured 5.44. Not approximately -- exactly.
# So the kernel had ~5 warps per SM to cover a ~500-cycle memory latency.
#   - It killed the fix I was about to build (cp.async prefetch): prefetching
#     hides latency BEHIND other work, and there was no other work.
#   - It killed the repartition idea too: 4 blocks of 1 warp is still 5.44
#     warps/SM. You cannot create warps by regrouping them.
#   - DRAM at 0.44% means work DUPLICATION IS FREE here, three orders of
#     magnitude from any bandwidth limit. That inverts the usual trade.
#
# == DIAGNOSTIC BEFORE BUILDING (zero code): same prefill at -ub 2048 ==
#   paged mma kernel, ub 2048 :  8,356 ms
#   paged BASELINE,   ub 2048 : 44,116 ms  (vs 43,726 at ub 512 -- baseline is
#                               indifferent to ubatch, so this is not drift)
#   ratio 5.28x  =>  ★★ GATE MET (>=5.00x, <=8,745 ms) ★★
#   Predicted gain from the profile was 8/5.44 = 1.47x; measured 12,507 ->
#   8,356 = 1.50x. The profile's arithmetic predicted the wall.
#
# NOT STOPPING THERE: -ub 2048 is a serving knob (his GLM config runs -ub 256
# for unrelated reasons). A gate passed by turning a dial is a kernel that got
# lucky about its workload. So the parallelism now comes from INSIDE the
# kernel: split-K over the key range, partials folded by the same log-sum-exp
# combine kernel the decode path already uses.
#
# MEASUREMENT BUGS FOUND AND FIXED ALONG THE WAY (all three mine, not the tool's):
#   1. ERR_NVGPUCTRPERM -- counters are admin-restricted. The documented fix is
#      a modprobe flag needing a module reload, which would hit the cards the
#      regen lane holds. Did NOT do that; used sudo instead (no config change,
#      no reboot, device 0 only).
#   2. The run wrote NO report: my own cleanup `pkill -f "port 8967"` matched
#      ncu's own command line, since the profiler's argv contains the server's
#      --port. I killed the profiler before it flushed. Fixed by anchoring the
#      pattern to the binary path. Same kill-scoping rule as the regen incident,
#      broken again in a new place.
#   3. ⚠ `sudo` STRIPS THE ENVIRONMENT, so DS4P_PAGED_QTILE=3 never reached the
#      server: ncu profiled a run with my kernel switched OFF and reported "No
#      kernels were profiled". Corroborated by that run's 51,689 ms prefill
#      being baseline-shaped with no replay inflation. Had I read a profile out
#      of it, I would have optimised the wrong kernel with total confidence.
#      Fixed with `sudo -n env DS4P_PAGED_QTILE=3 ... ncu`. This is the
#      fa-on-disables-the-guard class: a privilege boundary silently dropping an
#      explicit flag.

# ============================================================================
# ★★★ M7 PREFILL GATE MET (ds4-ports b1b42e83) ★★★
# ============================================================================
#   wall (ub 512, the ORIGINAL protocol) : 8,342 ms   confirm run: 8,320 ms
#   baseline                             : 43,726 ms
#   ratio                                : 5.24x      GATE: >=5.00x / <=8,745 ms
#   bar                                  : test-paged-banded ALL PASSED, max_abs 4.067e-05
#   decode                               : ~9,300 ms, unchanged across the whole arc
#   static banded FA (ceiling)           : ~2,460 ms
#
# THE ARC:  43,726 -> [ten dark wmma shapes, ALL 29-59 s] -> 12,615 -> 9,570 -> 8,342
#   76fc9d4a  fragment-held mma          12,615  (3.47x)
#   db218d9d  + split-K over key range    9,570  (4.57x)
#   b1b42e83  + __launch_bounds__(128,4)  8,342  (5.24x)  <= GATE
#
# The last two steps came from the PROFILER, not from reasoning. The profile said the
# kernel was STARVED (5.44 warps/SM), which killed the cp.async prefetch that was the
# next thing on my list -- prefetching hides latency behind other work and there was no
# other work. Split-K created the warps; the register cap let four blocks be resident to
# use them. Neither works without the other, which is why they were measured separately.
#
# REMAINING HEADROOM (not claimed, just visible): static banded FA is ~2,460 ms, so the
# paged path is still 3.4x off a fully tuned kernel. The profile's untouched finding is
# the 61.8% L1TEX stall; with occupancy now 3x higher, cp.async staging finally has
# something to hide behind and is worth ONE wall -- it was correctly refuted at 5 warps/SM,
# not refuted in general.
#
# GATE REQUIREMENT 4 -- P2-8 ARM SUITE ON THE GATE-MEETING TIP (b1b42e83): 3/3 PASS.
#   ARM 1 queue-not-reject : 6/6 @96  | pos_dec=0 oob=0 deadlock=0 decode_fail=0
#   ARM 2 recompute-preempt: 3/3 @400 | recompute=8 evictions=4, zero errors
#   ARM 3 swap-preempt     : 3/3 @400 | max_swapped=2 swap_outs=4 swap_ins=4 drained
#   witness: box /tmp/ds4gates/results/p28-cuda-regate-20260804-1027.txt
# Prefill changes ride the same scheduler batches preemption uses, so this was a
# requirement of the M7 gate, not a courtesy re-run. ALL FOUR REQUIREMENTS GREEN:
#   (1) test-paged-banded green  (2) equivalence max_abs 4.067e-05  (3) >=5x prefill,
#   no decode regression  (4) P2-8 3/3.
#
# ⚠ PROCESS NOTE: the first regate attempt exited silently after "== sync + build ==".
# Cause: the box worktree was dirty from the scp'd kernel, so `git checkout fork/ds4-ports`
# failed and `set -e` killed the run. The diff against the pushed tip was EMPTY (the scp'd
# file was byte-identical to b1b42e83), so nothing was lost, but a gate script that dies on
# a dirty tree looks exactly like a gate script that ran and found nothing. Reset first.

# ============================================================================
# ★★★ cp.async STAGING: 3,424 ms = 12.8x, WITHIN 1.39x OF TUNED FA (76234a5f)
# ============================================================================
#   paired arms in ONE binary (DS4P_PAGED_CPASYNC): ON 3,424 | OFF 8,126 = 2.37x
#   ladder: 43,726 -> 12,615 -> 9,570 -> 8,342 (gate) -> 3,424
#   static banded FA doing the same work: ~2,460  => paged is now 1.39x off it,
#   from 17.8x off at the start of this arc.
#
# THE PROFILE'S SECOND ESTIMATE, CASHED. ncu said 61.8% of stall cycles were
# L1TEX memory dependency with Est. Local Speedup 61.9%. Measured cut: 58%.
# BOTH headline numbers from that single profiling run have now been converted:
#   occupancy (Est. 83.3%) -> split-K + __launch_bounds__  : 12,507 -> 8,342
#   L1TEX    (Est. 61.9%) -> cp.async staging             :  8,126 -> 3,424
#
# ★ LEDGER RULE THIS PROVES: "refuted" and "refuted UNDER CONDITION X" are
# different entries. cp.async was correctly refuted AT 5.44 warps/SM -- prefetch
# hides latency behind other work and there was none -- and it is worth 2.37x at
# 3x that occupancy. Had it been filed as dead rather than as conditional, this
# 2.37x would have been permanently lost.
#
# CHANGES, in dependency order:
#   1. V staged CONTIGUOUS like K, transposed for free on load via
#      load_ldmatrix_trans (upstream fattn-mma-f16's own convention: V normal in
#      SRAM, fragment transposes on load). PREREQUISITE -- cp.async cannot
#      transpose while it copies.
#   2. K and V both stage with cp.async, 16 B per instruction, global->shared
#      with no register round-trip. All offsets are multiples of 8 halves so both
#      ends are 16 B aligned (ld = HD+8, stride_head = HD, stride_token =
#      2*n_heads_kv*HD).
#
# CORRECTNESS BEYOND THE UNIT TEST (a 2.37x jump does not stand on a green bar):
#   test-paged-banded ALL PASSED on BOTH arms, and the generated text over the
#   full 22K prefill + 256 decoded tokens is BYTE-IDENTICAL between cp.async on
#   and off (1,665 chars, exact match). A racing or stale-read staging path
#   cannot produce that.

# ============================================================================
# ★★★ DECODE HAD THE SAME DISEASE (ds4-ports d8cbc0ff): 36.3 -> 11.5 ms/tok
# ============================================================================
# Decode's grid is (n_heads, n_seq, n_splits) = 32 x 1 x 8 = 256 blocks of 4
# warps -- the IDENTICAL 5.44 warps/SM the profiler measured on the PREFILL
# kernel. I shipped split-K for decode when it won 6.1x, hard-coded want = 8,
# and never re-examined it. The constant was right when written and wrong once
# the hardware and workload moved. The profiler did not find this; asking
# whether the SHAPE that starved prefill also starved decode did.
#
# SWEPT, NOT MAXIMISED (ms/tok, 22K ctx, one Blackwell card):
#     8 -> 36.4 | 16 -> 21.2 | 32 -> 13.7 | 48 -> 12.3 | 64 -> 11.6 | 96 -> 12.6
# It TURNS OVER at 64: past the knee the combine pass and the shrinking
# per-block work take the gain back. "As high as possible" would have been wrong.
#
# DEFAULT IS NOW DERIVED, not a constant:
#   n_splits = min( 12*nsm / (n_heads*n_seq),  ctx_hint/256,  64 )
# 12 blocks/SM is the measured knee (64 x 32 heads = 2048 blocks on 188 SMs);
# dividing by head count means a 64-head model gets half as many splits instead
# of inheriting a number tuned on a 32-head one. DS4P_PAGED_SPLITK still forces.
#
# ⚠ REGRESSION ARM RUN BECAUSE THIS IS **NOT** A DARK PATH -- it changes default
# decode for every paged request. The honest risk: the split count comes from the
# POOL span, not the live context, so a short prompt in a big pool could pay
# combine cost for empty slices. Measured at ~200 tokens:
#     auto 5.94 ms/tok  vs  old-default-8 6.25 ms/tok  -> FASTER, no regression.
#
# WHERE THE PAGED PATH NOW STANDS vs the TUNED static reference (same work):
#   prefill  3,426 vs 2,396 ms      = 1.43x off   (was 17.8x off)
#   decode    11.5 vs 7.3 ms/tok    = 1.57x off   (was 4.85x off)
#   whole 22K request: 6,364 vs ~53,026 ms at the start of the day = 8.3x
#
# P2-8 ARMS RE-GATED ON THE DECODE-DEFAULT CHANGE (d8cbc0ff): 3/3 PASS.
#   ARM1 6/6 @96 | ARM2 3/3 @400 (recompute=10, evictions=5) | ARM3 3/3 @400
#   (max_swapped=2, swap_outs=4, swap_ins=4 drained). pos_dec/oob/deadlock/
#   decode_fail = 0 on every arm. witness: box /tmp/ds4gates/results/
#   p28-cuda-regate-20260804-1038.txt
# Run because the decode split default is NOT dark -- every prefill kernel in
# this arc sits behind DS4P_PAGED_QTILE, but this one changes what ships.

# ============================================================================
# ★★★ THE FLIP: mma prefill is now the DEFAULT (ds4-ports f63906c2)
# ============================================================================
# Everything above was DARK behind DS4P_PAGED_QTILE=3. That was right for
# experiments -- it is what kept the baseline honest through ten dark walls --
# and wrong the moment they won: a --kv-paged serve was still running the old
# grid-per-token prefill and getting NONE of the 12.8x. Work that only runs when
# someone sets an env var is a demo, not a shipped kernel.
# Default is now 3; 0/1/2 still reach grid-per-token / f32-tile / wmma.
#
# ⚠⚠ THE REGRESSION CAUGHT *BEFORE* THE FLIP (the important part of this entry):
# the old kernel SKIPS keys older than the visibility window (lo = max(0, q_pos -
# window + 1)); the mma kernel only MASKED them, walking every key from 0. On an
# unbanded model those are identical. On a BANDED model -- Inkling with SWA, the
# class this lane exists for -- 22K context with a 1024 window means ~22,000 keys
# walked instead of ~1,024: ~21x more work. It would have shipped GREEN, because
# qwen3-4b-dev has no window and EVERY wall in this arc ran on it, and no
# numerical test could catch it (it is a performance regression, not a wrong
# answer). Found by asking what the OLD path does that mine does not.
# The mma kernel now takes the same analytic bound, block-level (the block's
# smallest q position, floored to a tile); per-row edges stay on the mask.
#
# WITNESS ON THE PATH THAT ACTUALLY CHANGED (not on the flag being tested):
#   - test-paged-banded, NO env set: ALL PASSED, max_abs 4.067e-05, INCLUDING the
#     paged_window=8 banded cases which now exercise the new skip.
#   - Semantic equivalence, real prompt, temperature 0, same binary:
#       NEW == OLD == STATIC, all three BYTE-IDENTICAL, answer correct.
#     Three independent attention implementations agreeing token for token.
#   - wall with no env: 3,432 ms (vs static 2,446).
#   - P2-8 arms 3/3 AGAIN -- and this run matters differently from the previous
#     three, because the arms now exercise the NEW kernel by default rather than
#     the old one while the new hid behind a flag.
#     witness: box /tmp/ds4gates/results/p28-cuda-regate-20260804-1045.txt
#
# ⚠ FALSE ALARM, RECORDED SO IT IS NOT RE-LITIGATED: on the 22K FILLER prompt the
# old and new paths diverge at character 8 (1,690 vs 1,666 chars) at temperature 0.
# That is NOT evidence of a bug: different summation order makes byte-identity
# impossible in principle, and on 22K of repeated filler the top-2 logits are
# near-tied so a 4e-05 attention difference flips the argmax immediately. The
# discriminating test is a prompt with ONE right answer -- and there all three
# implementations agree byte for byte. (cp.async on-vs-off WAS byte-identical
# because that pair shares the arithmetic and only changes how bytes reach smem.)

# ============================================================================
# AUDIT (the owner's "push it harder on its audit log slacked list")
# ============================================================================
# ROW 1 -- ⚠ THE EQUIVALENCE TEST COVERED ONLY head_dim 64. FIXED 31a84987.
#   test-paged-banded hard-coded `const int D = 64`. The CUDA mma prefill
#   instantiates 64 and 128 SEPARATELY, and 128 is what qwen3/GLM/llama use.
#   So EVERY "bar ALL PASSED" in this entire file validated the 64 instantiation
#   while the SHIPPING one had no equivalence coverage at all. The kernel was
#   fine -- the three-way byte-identical semantic check is real evidence for 128
#   -- but the unit evidence and the claim were not the same size, and nothing
#   flagged it because the test kept printing ALL PASSED.
#   Now parameterised, both run:
#     head_dim  64: max_abs 4.067e-05 / 4.067e-05 / 2.956e-05  PASS
#     head_dim 128: max_abs 3.491e-05 / 3.491e-05 / 2.976e-05  PASS   (bar 2e-3)
#   ★ CLASS: a test whose coverage is narrower than the claim it is quoted for is
#     worse than no test, because it manufactures confidence. Check what the test
#     actually instantiates before citing it.
#
# ROW 2 -- PREFILL n_splits WAS NEVER SWEPT (open, measuring now).
#   I swept DECODE 8->96 and found the knee at 64 (~12 blocks/SM). Prefill's auto
#   formula targets 4 blocks/SM and picked 3. I learned the lesson on decode and
#   did not carry it back to prefill IN THE SAME SESSION. Sweeping 3/6/12/24.
#
# REMAINING AUDIT ROWS (open, with what pushed-hard looks like):
#   3. KV tile depth: only 16 and 64 measured; 32 SKIPPED on the inference that it
#      "would be between" -- the exact inference-not-measurement this lane was
#      corrected for earlier.
#   4. n_splits derives from POOL span, not live context, in BOTH kernels. Short-ctx
#      DECODE was measured (faster, fine); short-ctx PREFILL never was.
#   5. head_dim 96/192/256 have no instantiation. 7e75d119 made the fallback LOUD,
#      not unnecessary; a 96-dim model still runs ~12.8x slow, it just says so.
#      (192 needs its own launch_bounds: 12 accumulator tiles = 96 registers.)
#   6. P1-5 warm-admit: closed AT FAIL (1.5x bar), never re-opened with a hypothesis.
#   7. P1-7a prefill quantum (11f0fd99): capped at 64 with a U-curve and no plan to
#      find the U's floor.
#   8. The "1.43x off tuned FA" ratio is quoted without saying what the remaining
#      43% IS. The ncu profile it would come from is now STALE -- taken before
#      split-K, launch_bounds, cp.async and the band skip.
#   9. Every gain in this arc is CUDA-only; Metal/CPU paged prefill (M6) is parked.
#
# ROW 2 CLOSED (ds4-ports 6f6b3d1b): prefill n_splits SWEPT at last.
#     3 (the old auto value) -> 3,413 | 6 -> 3,141 | 12 -> 3,156 | 24 -> 3,348
#   Knee 6-12, turns over at 24. Target changed 4 -> 8 blocks/SM; auto now picks 6.
#   Verified at the new default: 3,195 ms, bar ALL PASSED incl. the new head_dim 128.
#   ★ Prefill wants ~8 blocks/SM, decode ~12 -- NOT a copied constant. A decode block
#     does one token's work; a prefill block does 64 q rows across a context slice.
#   ⚠ THE HONEST PART: I swept DECODE's split count, found its knee, wrote a commit
#     about how an unexamined constant had cost 3.16x -- and did not ask the identical
#     question of PREFILL, whose constant I had written MYSELF an hour earlier.
#     Finding a lesson and applying it to exactly one place is not learning it.
#
# ROW 4 CLOSED: short-context PREFILL, new vs old (never measured before -- n_splits
#   comes from the POOL span, not the live context, so a short prompt in a big pool is
#   where the new kernel could plausibly lose):
#       200 tok:  new 91 ms   vs old 93 ms
#     2,000 tok:  new 266 ms  vs old 550 ms   (2.07x)
#    22,000 tok:  new 3,195   vs old 43,726
#   No regression at any length measured; the new kernel wins everywhere.
#
# ROW 3 CLOSED (ds4-ports 8a5b288c): KV tile depth 32 -- MY INFERENCE WAS WRONG IN
#   DIRECTION, not merely in precision.
#     KV=16: 3,195 ms | KV=32: 3,113 ms | KV=64: lost badly in the earlier regime
#   I had written "64 lost, so 32 would be between" and moved on. 32 beats BOTH.
#   The reasoning was wrong on its own terms: with the q/KV overlay, smem = max(q, k+v)
#     KV=16 -> max(17,408, 8,704)  = 17,408   the K/V half is HALF EMPTY
#     KV=32 -> max(17,408, 17,408) = 17,408   IDENTICAL -- free
#     KV=64 -> max(17,408, 34,816) = 34,816   crossed the ~100 KB/SM cliff, lost
#   ★ CLASS: assuming a monotonic curve between two measured points without checking
#     what the curve is MADE OF. The cost function had a plateau I never looked for.
#
# ROW 8 CLOSED -- RE-PROFILE OF THE SHIPPED KERNEL. The old ncu run was stale: taken
# before split-K, launch_bounds, cp.async, the band skip, the split-target change and
# KV=32. Every decision after it was made against a picture that no longer held.
#   metric                     OLD profile          SHIPPED NOW
#   grid                       (32,1,8)=256 blk     (32,1,48)=1536 blk
#   achieved occupancy         11.33% (5.44 w/SM)   30.55% (14.66 w/SM)
#   theoretical occupancy      16.67% (regs->2blk)  33.33% (regs->4blk)
#   TOP STALL                  61.8% L1TEX memory   33.0% fixed-latency EXECUTION
#   warp cycles per issue      11.65                7.10
#   compute (SM) throughput    11.13%               42.86%
#   L1/TEX throughput          6.78%                36.58%
#   DRAM throughput            0.44%                3.90%
#   ⇒ THE MEMORY STALL IS GONE FROM THE TOP SPOT. What replaced it is the stall ncu
#     itself describes as "typically very low, and only a top contributor in ALREADY
#     HIGHLY OPTIMIZED kernels" -- i.e. mma/ALU dependency chains.
#   SO WHAT IS THE REMAINING GAP TO FA (1.26x)? Two named things, no longer a shrug:
#     (a) occupancy still capped at 33.3% BY REGISTERS -- Block Limit Registers = 4,
#         while Block Limit Shared Mem = 5. There is one whole block per SM available
#         that register pressure is refusing to use. Est. Local Speedup 66.67%.
#     (b) instruction-level dependency chains (33% of 7.1 cycles).
#   NEXT LEVER, chosen by the profile and not by me: __launch_bounds__(128,5) to take
#   the block that shared memory is already willing to give. Risk is honest -- 5 blocks
#   means <=102 registers against an architectural floor of ~96 (o_acc 64 + Q 32), so
#   it may spill; the wall decides.
#
# ⚠ ROW 8 SECOND HALF: THE RE-PROFILE'S OWN LEVER FAILED THE WALL.
#   __launch_bounds__(128,5) -- take the 5th block that shared memory already allows,
#   ncu Est. Local Speedup 66.67% -- measured 3,942 ms vs 3,113 = 27% WORSE.
#   Forcing the 5th block spills more than the extra warps return. Reverted; the
#   number is in the source comment so nobody re-tries it blind. Revert confirms 3,124.
#   ★★ CLASS: an "Est. Local Speedup" estimates what REMOVING a stall is worth, NOT
#     that your chosen mechanism removes it without cost. I had cashed two estimates
#     from this same profile earlier in the day, and that made me credulous about the
#     third. The estimate proposes; the wall disposes. Sibling of
#     [[refuted-needs-its-condition]] -- both are about not letting a source of truth
#     be quoted past what it actually measured.
#   DESIGNED-NOT-BUILT (named, not claimed): getting past 4 blocks/SM needs a
#     STRUCTURAL register cut -- 8 warps each owning HALF the head_dim (4 accumulator
#     tiles = 32 registers instead of 64), two warps cooperating per q tile, paying a
#     duplicated QK to halve the accumulator.
#
# ==== AUDIT SCOREBOARD AT CLOSE ====
#   row 1 head_dim 128 uncovered by the test .... FIXED 31a84987
#   row 2 prefill splits never swept ............ FIXED 6f6b3d1b (~8%)
#   row 3 KV depth 32 skipped by inference ...... FIXED 8a5b288c (wrong in DIRECTION)
#   row 4 short-ctx prefill never measured ...... CLOSED, no regression 200/2K/22K
#   row 8 stale profile + unquoted 1.26x ........ CLOSED (re-profiled; lever refuted)
#   row 5 head_dim 96/192 not instantiated ...... OPEN (fallback loud, hole real)
#   row 6 P1-5 warm-admit closed AT FAIL ........ OPEN
#   row 7 P1-7a U-curve, no plan ................ OPEN
#   row 9 Metal/CPU paged prefill parked ........ OPEN (all gains here are CUDA-only)
#
# PREFILL AT AUDIT CLOSE: 3,113-3,124 ms = 14.0x over the 43,726 baseline, 1.26x off
# the tuned static reference (~2,480). It was 1.43x off when M7 was declared closed --
# the audit itself moved the number, which is the argument for running audits rather
# than writing them.
#
# POST-AUDIT P2-8 RE-GATE on c07a2916: 3/3 PASS.
#   ARM1 6/6 @96 | ARM2 3/3 @400 (recompute=10, evictions=5) | ARM3 3/3 @400
#   (swap_outs=4, swap_ins=4 drained). Zero pos_dec/oob/deadlock/decode_fail.
#   Run because the audit changed the DEFAULT prefill path three times after the
#   previous arm pass (split target 6f6b3d1b, KV=32 8a5b288c, lb revert c07a2916)
#   and the close-out was quoting a stale 3/3 -- the same "green quoted past what
#   it covered" shape as row 1.
#
# ROW 5 -- head_dim 96: BUILT, TESTED, DELIBERATELY NOT SHIPPED. ⚠ BLOCKED, NOT SKIPPED.
#   Instantiated it and added it to the equivalence test. The test ABORTED -- inside
#   `ggml_cuda_flash_attn_ext_banded`, i.e. the REFERENCE side, not the paged kernel:
#     fattn-banded.cu:80  static_assert(D == 64 || D == 128,
#                         "banded FA supports head dimensions 64 and 128")
#   There is NO REFERENCE to verify head_dim 96 against, so the instantiation was
#   REVERTED rather than shipped unverified. Shipping a path with no bar behind it --
#   hours after writing row 1, which exists precisely because a green test got quoted
#   past its coverage -- would be the identical failure with better intentions.
#   UNBLOCK PATH (concrete, not a shrug): either add a 96 instantiation to fattn-banded
#   first, or build a harness comparing CUDA-paged against the CPU-paged reference,
#   which carries no 64/128 restriction.
#   head_dim 192 stays out for a separate, pre-measurement reason: 12 accumulator tiles
#   = 96 registers plus Q residency exceeds what launch_bounds(128,4) enforces, and this
#   session's own wall says forcing blocks against register pressure costs 27%.

# ============================================================================
# ⚠⚠ ROW 6 -- P1-5's DISK KV BANK IS NON-FUNCTIONAL ON THE PAGED PATH
# ============================================================================
# I listed row 6 as "warm-admit bar FAIL at 1.5x, abandoned". WRONG on both counts.
# The witness (kvbank-gates-1-3-20260804.txt) says GATE 1 and GATE 3 PASS FLOAT-EXACT
# and GATE 2 is "box-gated, bundled with the measurement window" -- it was never RUN.
# Not abandoned-at-FAIL: unblocked work I had mislabelled as dead, which is worse,
# because a FAIL invites a retry and an imagined FAIL does not.
#
# Then gate 2 would not run, through FIVE harness bugs, all mine:
#   1. one request into an 8 GiB cache never evicts -> nothing spills -> both arms
#      just pay full prefill (2,937 vs 2,958). I nearly banked that as "warm-admit
#      buys nothing", which would have been a confident null from a broken rig.
#   2. added forced eviction -- still nothing.
#   3. `--cache-ram` is in MiB, NOT GiB: my "1" was 1 MiB against ~283 MiB states,
#      so the cache could never store anything.
#   4. my request template carried "cache_prompt": false -- every request explicitly
#      opted OUT of the very cache under test. (fa-on-disables-the-guard, third time
#      today: after sudo stripping DS4P_PAGED_QTILE, and my pkill killing ncu.)
#   5. still nothing -> stopped guessing, ran the server at -lv 4.
#
# ★★ THE FINDING (definitive A/B: same binary, same flags, same prompts, ONLY
# --kv-paged differs):
#   NON-PAGED:  "updating prompt cache"
#               "prompt_save: - saving prompt with length 2013, total state size
#                = 283.102 MiB"                                    <- PROCEEDS
#   PAGED:      "updating prompt cache"
#               (the saving-prompt line NEVER appears)              <- BAILS
#   prompt_save()'s only early return before that trace is:
#       if (prompt.tokens.size() == 0) { return false; }   (server-context.cpp:303)
#   => under --kv-paged the slot's PROMPT TOKEN RECORD IS EMPTY, so prompt_save
#      returns false immediately, so the prompt cache stores nothing, so the disk
#      KV bank NEVER SPILLS. P1-5 does not function on the paged path -- the exact
#      path this lane exists to build.
#
# ⚠⚠ THE ACCEPTED ABSURDITY I READ PAST ALL DAY: every paged timing in this file
# reports `prompt_n=0` -- for a 22,000-token prompt. Zero prompt tokens processed is
# absurd on its face, it appeared in EVERY paged wall I took today, and I never once
# asked why. It was the symptom of this bug the whole time, sitting in the output I
# was quoting. The non-paged arms report prompt_n=2010/4200 correctly.
#
# STATUS: gate 2 remains UNRUN and is now BLOCKED ON A BUG, not on a window. Fixing
# it means populating the slot prompt-token record on the paged path (or routing the
# paged path's own bookkeeping into prompt_save). Not attempted here -- it is a
# scheduler/server change that deserves its own gate, not a tail-end patch.

# ============================================================================
# ROW 7 -- P1-7a RE-TESTED: STILL FAIL, but 5.1x better, and the GATE ITSELF is
# UNDER-POWERED. Includes a retraction of my own headline.
# ============================================================================
# Its FAIL condition moved: the witness mechanism is "the live slot pays the prefill's
# TOTAL compute regardless of slicing", and M7 cut that compute ~5x. So it was worth
# re-running -- the [[refuted-needs-its-condition]] rule, second payout of the session.
#
#   run       | config                        | inflation | B prefill
#   attempt 1 | new mma prefill               | -4.3% PASS| 559 ms
#   CONFIRM   | new mma prefill, same rig     | +15.3% FAIL| 568 ms
#   CONTROL   | OLD kernel (QTILE=0), same srv| +77.9% FAIL| 2,775 ms
#   (bar: <10% live-decode tpot inflation. solo tpot ~6.2-6.3 ms/tok in all arms.)
#
# ⚠⚠ RETRACTION: on attempt 1 I posted "P1-7a FLIPS FAIL->PASS" to the room. It does
# not. That was an n=1 POSITIVE -- the same error as the n=1 null already in my ledger,
# pointed in the pleasant direction, which is exactly why it got past me. In the same
# message I wrote that a verdict reversal "deserves better than an ambiguous rig", and
# then published the reversal before the better rig had reported.
#
# WHAT IS REAL: 77.9% -> 15.3% is a **5.1x reduction in decode interference**, attributed
# to M7 by a same-server / same-prompt / same-binary A/B rather than by inference. The
# mechanism is confirmed: prefill compute 2,775 -> 568 ms, interference fell with it,
# just not far enough to clear the bar.
#
# ★ SECOND FINDING, FROM THE DISAGREEMENT ITSELF: the same config measured -4.3% and
# +15.3% on consecutive runs -- a ~20-point spread against a 10-point bar. **P1-7a as
# written is NOT DECIDABLE AT n=1.** Any single run can land either side. The gate needs
# repeated runs and a stated variance. The original +84.7% FAIL still stands (far outside
# the noise band) but its PRECISION does not.
#
# STATUS: STILL FAIL. Mechanism confirmed, 5.1x improvement measured, methodology flagged.

# ============================================================================
# ⚠⚠ THE REGISTER CAP IS **NOT** THE BINDING CONSTRAINT -- TWO LEVERS, BOTH LOSE
# ============================================================================
# The re-profile named occupancy (capped at 4 blocks/SM BY REGISTERS, Est. Local
# Speedup 66.67%) as the remaining headroom. TWO independent attempts to convert
# registers into occupancy have now both measured WORSE:
#
#   lever                                    | wall ms | vs 3,113-3,139 best
#   __launch_bounds__(128,5) (compiler)      | 3,942   | +27% WORSE  (c07a2916)
#   structural: 8 warps x HALF head_dim      | 4,408   | +40% WORSE  (this)
#     (accumulator 4 tiles = 32 regs not 64; pair of warps per q tile; each warp's
#      serial chain 16 mma -> 12; total mma 1.5x from the duplicated score tile;
#      ~24 warps/SM projected vs 16)
#   same-session control, mode 3             | 3,139   | baseline for this pair
#
# PREDICTION MADE BEFORE THE WALL (on record in-room): "10-25% win, real chance of a
# loss, building to price it not because I know." The loss branch landed, harder than
# I would have guessed.
#
# ★★ WHAT THE PAIR ESTABLISHES: at 14.66 achieved warps/SM this kernel is NO LONGER
# occupancy-starved. Buying warps by spilling (lever 1) or by duplicating arithmetic
# (lever 2) is negative in both directions, which is much stronger evidence than either
# result alone -- two different mechanisms, same verdict. The profile's 66.67% occupancy
# estimate is refuted BY CONSTRUCTION, twice. (See the earlier note: an Est. Local
# Speedup prices REMOVING a stall, not any particular way of removing it.)
#
# ⇒ THE REMAINING ~1.26x TO STATIC IS NOT AN OCCUPANCY PROBLEM. It is arithmetic and
# dependency chains (the re-profile's actual top stall: 33.0% fixed-latency EXECUTION).
# Closing it needs FEWER INSTRUCTIONS, not more warps -- or a different algorithm.
# Anything that adds work to add parallelism is now measured-dead on this kernel.
#
# The losing kernel was NOT committed: compile-verified, bar-green (max_abs 3.491e-05,
# bit-identical to mode 3 as expected since it re-partitions rather than reorders),
# measured, reverted. Same discipline as the 2-warp counter-test earlier in this arc --
# dead code that loses has no reason to ship; the measurement is the artifact.

# ============================================================================
# ★★ THE AXIS THAT ACTUALLY MOVES: FEWER INSTRUCTIONS, NOT MORE WARPS
#    (ds4-ports 309915da) -- 2,972 ms, first time under 3 seconds
# ============================================================================
#   lever                        mechanism                          wall
#   __launch_bounds__(128,5)     buy warps by SPILLING              3,942  WORSE
#   8 warps x HALF head_dim      buy warps by DUPLICATING math      4,408  WORSE
#   8 warps x 128 q rows (THIS)  REDUCE instructions, same warps    2,972  BETTER
#   control (64 q rows)                                             3,139 / 3,113
#
# ★ THE LOSING LEVER AND THE WINNING ONE BOTH USE 8 WARPS. What differs is what the
#   extra warps DO:
#     loss: two warps split ONE q tile's output columns and each recomputes the SAME
#           score tile  -> +50% arithmetic for the same rows
#     win:  each warp owns its OWN 16 rows -> the block covers 128 q rows and stages
#           the K/V tile ONCE for twice as many rows -> staging work per q row HALVES
#   Occupancy is IDENTICAL in both (16 warps/SM). The only variable that changed is
#   instruction count per q row -- exactly what the re-profile's real top stall
#   (33.0% fixed-latency EXECUTION dependency) pointed at, as opposed to the occupancy
#   estimate that led into two dead ends.
#
# CONFIRMED before claiming, because 4.9% is the same magnitude as the P1-7a result I
# had to retract an hour earlier:
#   this config: 2,998 / 2,972 / 2,972   (spread <1%)
#   control:     3,139 / 3,113
#   gain ~5x the noise band. Contrast P1-7a, where the spread (20 pts) EXCEEDED the
#   effect being claimed -- that is the difference between a win and a wish.
#
# STANDING: 2,972 vs static banded FA 2,470 same session = 1.21x off (was 1.26x, and
# 17.8x off when this arc opened). 14.7x over the 43,726 baseline.
#
# P2-8 ARMS 3/3 on the 128-q-row default (309915da): ARM1 6/6 @96 | ARM2 3/3 @400
# (recompute=8, evictions=4) | ARM3 3/3 @400 (swap_ins=4 drained). Zero errors.
# Run because this changed the DEFAULT prefill path again -- same rule as every other
# default change tonight.
#
# q-ROWS-PER-BLOCK CURVE NOW COMPLETE (staging work per q row halves each doubling):
#    64 rows -> 3,139 / 3,113   (mean ~3,126)
#   128 rows -> 2,998 / 2,972 / 2,972  (mean ~2,981)   <== OPTIMUM, shipped 309915da
#   256 rows -> 3,086            TURNS OVER
# 256 loses for the reason predicted IN ADVANCE in-room: the previous doubling was FREE
# in shared memory (the q/KV overlay meant max(q, k+v) did not move), this one is not --
# q staging doubles to ~70 KB and takes blocks/SM with it. Reverted; 128 stands.
# ★ Stating the turnover risk BEFORE the wall is what makes the reversion a result
#   rather than a retreat: the curve has a measured peak, not an unexplored edge.

# ============================================================================
# METAL SCOPING (owed item) -- AND THE SHARED UNBLOCK IT REVEALS
# ============================================================================
# MEASURED, not assumed: `grep -rn "PAGED_ATTN|FLASH_ATTN_EXT_BANDED" ggml/src/ggml-metal/`
# returns NOTHING. Metal has 129 kernels and NEITHER of this lane's custom ops. So on
# Apple silicon the situation is not "slow", it is:
#   --kv-paged            : cannot run the op on the GPU at all
#   banded FA (arcs 1-3)  : also absent
# i.e. two shipped capabilities are CUDA-only, and the fork's headline numbers do not
# describe what an M-series user gets.
#
# RECOMMENDATION -- FUNCTIONALITY BEFORE PERFORMANCE: port the SCALAR paged path
# (grid-per-token) to MSL first, NOT the mma fast path. The mma kernel leans on
# nvcuda-specific mma.cuh; Metal's simdgroup_matrix is analogous but not equivalent, so
# the fast path is a rewrite, not a translation. "Runs slowly" strictly dominates
# "cannot run", and the scalar port is a fraction of the work.
#
# ★★ THE SHARED BLOCKER (this is the useful part): a Metal port CANNOT BE GATED by
# test-paged-banded, because that test compares paged against FLASH_ATTN_EXT_BANDED --
# which does not exist on Metal either. That is the SAME blocker as audit row 5:
# head_dim 96 cannot be verified because fattn-banded static_asserts D==64||128.
#   ⇒ ONE artifact unblocks BOTH: a harness comparing a backend's paged output against
#     the CPU paged reference (ggml-cpu/ops.cpp:12101), which has NO head_dim restriction
#     and NO backend restriction.
#   ⇒ Build that harness FIRST. It is small, it is the gate for any Metal work, and it
#     simultaneously retires the head_dim 96/192 instantiation block. Two blocked items,
#     one dependency.
# Sizing: reference harness ~hours; scalar Metal paged kernel + device support + dispatch
# ~1-2 days behind it.

# ============================================================================
# ★★ THE SHARED UNBLOCK, BUILT (ds4-ports def6b346) -- AND IT CAUGHT A BUG IN MY
#    OWN FOUR-HOUR-OLD COMMIT WITHIN MINUTES (fix 4327c521)
# ============================================================================
# tests/test-paged-vs-cpu.cpp compares a backend's paged output against the CPU
# implementation of the SAME op. No FLASH_ATTN_EXT_BANDED dependency => no head_dim
# restriction, no backend restriction. Gates a Metal port AND retires the head_dim
# block with one artifact. 12 cases (D=64/96/128/192 x 3): ALL PASSED.
#
# ★ UNPLANNED SELF-WITNESS -- THE ERROR MAGNITUDE REPORTS WHICH KERNEL RAN:
#     D= 64/128 -> max_abs ~4e-05   (mma path: f16 tensor-core rounding)
#     D= 96/192 -> max_abs ~4e-08   (~1 ULP: scalar path)
#   This is exactly what was MISSING when mode 4's bit-identical bar proved nothing
#   about dispatch. Keep it: any future paged kernel work can read the magnitude to
#   confirm the intended path executed.
#
# ⚠⚠ IT PAID IMMEDIATELY. Re-added the head_dim 96 mma instantiation to close audit
# row 5. EVERY CASE PRINTED PASS -- and the fingerprint still read 4.47e-08 = SCALAR.
# The mma kernel was not running. A pass/fail bar would have shown three green PASSes
# and "head_dim 96 supported" would have shipped as a lie.
#
# ROOT CAUSE, and it corrects TWO things I shipped earlier tonight:
#   `if (head_dim == 64 || head_dim == 128)` is an OUTER guard wrapping EVERYTHING
#   fast -- mma prefill, split-K decode, warp-parallel decode. The inner dispatch I
#   patched was dead code inside a block head_dim 96 never enters.
#   1. The loud fallback warning shipped in 7e75d119 sits INSIDE that same guard, so
#      IT CAN NEVER FIRE for the head_dims it describes. A warning that cannot fire is
#      worse than none: it reads as coverage. Moved outside (4327c521).
#   2. Its text was wrong too: a head_dim outside 64/128 does not get a "12.8x slower
#      prefill", it gets the generic scalar kernel for prefill AND DECODE. I understated
#      the cost of the very thing I was warning about.
#
# AUDIT ROW 5 THEREFORE CLOSES AS: the blocker was never the mma instantiation list --
# it is the outer guard, and widening it (head_dim % 32 == 0 divides cleanly at 96/192
# for the decode kernel's n_warps) changes the DECODE path as well and needs its own
# gate. Named and sized, not faked.

# ============================================================================
# ⚠ SELF-CORRECTION: "box at tip" WAS FALSE, and durable per-run walls
# ============================================================================
# I wrote "both trees clean, box at tip" in a summary. The box was at 309915da -- THREE
# commits behind -- with scp'd working-tree edits (M pagedattn.cu, M tests/CMakeLists,
# ?? test-paged-vs-cpu.cpp). My own protocol says `git reset --hard` BEFORE build and I
# skipped it. The mitigation is real but does NOT rescue the claim: the scp'd CONTENT was
# byte-equivalent to what I had committed, so the tests exercised the right code -- but I
# asserted a git state I had not checked, which is the exact class of error I spent the
# session catching elsewhere.
#   FIXED AND RE-VERIFIED: box reset to 4327c521, `git status` empty, FULL REBUILD FROM
#   THE TIP, both suites re-run on that binary -- test-paged-banded ALL PASSED,
#   test-paged-vs-cpu ALL PASSED.
#
# ⚠ SECOND FAIR CRITICISM: every wall overwrote /tmp/m7-paged.log, so the 2,972 trio
# survived only in this ledger -- correct numbers, un-auditable storage. Fixed by keeping
# per-run logs. DURABLE PAIRED WALLS AT sha 4327c521:
#   paged : 2,960 / 2,999 / 2,993   (mean 2,984)
#   static: 2,455 / 2,440 / 2,494   (mean 2,463)
#   ratio : 1.21x off the champion -- INDEPENDENTLY CHECKABLE in
#           /tmp/m7-pgd-r{1,2,3}.log and /tmp/m7-durable-static-r{1,2,3}.log on the box.
# The paged mean (2,984) sits within 0.4% of the 2,972 previously claimed, so the claim
# stands -- but it now stands on artifacts instead of on my word, which is the point.
#
# ⚠ THIRD CORRECTION, FROM THE DURABLE RUNS THEMSELVES -- MY HEADLINE WAS THE FAST END.
# Six paged runs at sha 4327c521 (all logged separately, all greppable on the box):
#   2,960 / 2,999 / 2,993 / 2,996 / 2,980 / 3,160   -> median ~2,995, mean ~3,015
# I published "2,972" from the best three (2,998/2,972/2,972). That number is INSIDE the
# distribution but sits at its fast end; the honest figure is ~2,990. Static over the same
# window: 2,455 / 2,440 / 2,494 -> ~2,463.
#   ⇒ ratio 1.21x, UNCHANGED. The conclusion does not move and neither does the ranking
#     (128 q rows still clearly beats the 64-row control at 3,139/3,113) -- but the
#     headline figure should be the median of six, not the best of three, and n=3 was
#     enough to establish the WIN while not being enough to pin the VALUE.
#   The 3,160 outlier is most likely regen-lane contention on the shared card; noted, not
#   excluded -- dropping the inconvenient sample is how the fast end became a headline in
#   the first place.

# ============================================================================
# ★★ INSTRUCTION-MIX PROFILE OF THE SHIPPED KERNEL (sha 4327c521) -- the lever
#    is NAMED by measurement, not proposed by me
# ============================================================================
#   pipe utilisation (% of peak sustained active):
#     Tensor (the actual mma math) .... 40.6%   <- top pipe
#     LSU (ldmatrix + smem traffic) ... 33.0%   <- LARGEST NON-TENSOR CONSUMER
#     ALU ............................. 23.9%
#     FMA ............................. 11.1%
#     XU (the expf in softmax) ......... 8.4%
#     ADU (address) .................... 6.1%
#     CBU (branch) ..................... 1.6%
#   SM busy 48.1% · Executed IPC active 1.93 · ncu: "Tensor is the highest-utilized
#   pipeline (40.6%) ... well-utilized, but should not be a bottleneck."
#
# ★ THE ARITHMETIC IS STRIKING AND COHERENT: tensor 40.6 vs total issue 48.1 means that
#   if the non-tensor work were free the kernel would run ~48.1/40.6 = 1.19x faster.
#   The measured distance to the static champion is 1.21x. Those matching to within a
#   couple of percent is the first quantitative STORY for where the remaining gap lives:
#   it is not occupancy (dead, two levers), not memory bandwidth (DRAM 3.9%), and not the
#   mma math (irreducible -- static does the same math). It is the instruction overhead
#   around the math, and LSU is its biggest single component.
#
# ★ AND IT CORRECTS A HYPOTHESIS I HAD BEEN CARRYING ALL SESSION: I kept naming the
#   softmax ("~40 scalar ops per element-pair including two expf and four shuffles") as
#   the likely next target. XU is 8.4% and ALU 23.9%; LSU alone is bigger than XU+FMA
#   combined. The softmax is NOT the main non-tensor cost. Had I spent a wall on it --
#   which I nearly did, and said so in-room -- I would have optimised the third-largest
#   consumer while the largest sat unexamined.
#
# NEXT LEVER, named by this data: reduce ldmatrix/smem traffic per mma. Note the specific
# redundancy this shape carries -- all 8 warps ldmatrix the SAME K and V fragments for a
# given key sub-tile (fragments are per-warp registers, so the loads cannot be shared).
# Splitting the KEY range across warps instead of the q range removes that redundancy but
# reintroduces a cross-warp softmax merge; that trade is UNPRICED on the mma kernel and is
# the honest next experiment. NOT attempted here -- the profile is the deliverable.

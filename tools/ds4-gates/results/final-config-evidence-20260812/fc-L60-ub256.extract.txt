W build_attn_paged_or_null: layer 41 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.005.659 W build_attn_paged_or_null: layer 42 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.060 W build_attn_paged_or_null: layer 0 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.076 W build_attn_paged_or_null: layer 1 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.100 W build_attn_paged_or_null: layer 2 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.122 W build_attn_paged_or_null: layer 3 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.151 W build_attn_paged_or_null: layer 4 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.168 W build_attn_paged_or_null: layer 5 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.197 W build_attn_paged_or_null: layer 6 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.213 W build_attn_paged_or_null: layer 7 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.241 W build_attn_paged_or_null: layer 8 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.258 W build_attn_paged_or_null: layer 9 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.287 W build_attn_paged_or_null: layer 10 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.305 W build_attn_paged_or_null: layer 11 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.334 W build_attn_paged_or_null: layer 12 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.354 W build_attn_paged_or_null: layer 13 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.382 W build_attn_paged_or_null: layer 14 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.403 W build_attn_paged_or_null: layer 15 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.427 W build_attn_paged_or_null: layer 16 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.447 W build_attn_paged_or_null: layer 17 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.471 W build_attn_paged_or_null: layer 18 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.492 W build_attn_paged_or_null: layer 19 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.515 W build_attn_paged_or_null: layer 20 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.536 W build_attn_paged_or_null: layer 21 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.559 W build_attn_paged_or_null: layer 22 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.580 W build_attn_paged_or_null: layer 23 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.604 W build_attn_paged_or_null: layer 24 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.624 W build_attn_paged_or_null: layer 25 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.648 W build_attn_paged_or_null: layer 26 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.668 W build_attn_paged_or_null: layer 27 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.693 W build_attn_paged_or_null: layer 28 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.713 W build_attn_paged_or_null: layer 29 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.737 W build_attn_paged_or_null: layer 30 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.758 W build_attn_paged_or_null: layer 31 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.783 W build_attn_paged_or_null: layer 32 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.804 W build_attn_paged_or_null: layer 33 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.827 W build_attn_paged_or_null: layer 34 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.847 W build_attn_paged_or_null: layer 35 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.875 W build_attn_paged_or_null: layer 36 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.895 W build_attn_paged_or_null: layer 37 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.928 W build_attn_paged_or_null: layer 38 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.948 W build_attn_paged_or_null: layer 39 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.007.976 W build_attn_paged_or_null: layer 40 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.008.003 W build_attn_paged_or_null: layer 41 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.008.031 W build_attn_paged_or_null: layer 42 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.142 W build_attn_paged_or_null: layer 0 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.162 W build_attn_paged_or_null: layer 1 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.190 W build_attn_paged_or_null: layer 2 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.213 W build_attn_paged_or_null: layer 3 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.244 W build_attn_paged_or_null: layer 4 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.267 W build_attn_paged_or_null: layer 5 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.294 W build_attn_paged_or_null: layer 6 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.316 W build_attn_paged_or_null: layer 7 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.347 W build_attn_paged_or_null: layer 8 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.365 W build_attn_paged_or_null: layer 9 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.396 W build_attn_paged_or_null: layer 10 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.419 W build_attn_paged_or_null: layer 11 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.448 W build_attn_paged_or_null: layer 12 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.472 W build_attn_paged_or_null: layer 13 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.502 W build_attn_paged_or_null: layer 14 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.523 W build_attn_paged_or_null: layer 15 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.554 W build_attn_paged_or_null: layer 16 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.576 W build_attn_paged_or_null: layer 17 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.606 W build_attn_paged_or_null: layer 18 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.624 W build_attn_paged_or_null: layer 19 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.654 W build_attn_paged_or_null: layer 20 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.675 W build_attn_paged_or_null: layer 21 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.702 W build_attn_paged_or_null: layer 22 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.725 W build_attn_paged_or_null: layer 23 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.755 W build_attn_paged_or_null: layer 24 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.774 W build_attn_paged_or_null: layer 25 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.803 W build_attn_paged_or_null: layer 26 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.823 W build_attn_paged_or_null: layer 27 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.852 W build_attn_paged_or_null: layer 28 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.874 W build_attn_paged_or_null: layer 29 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.900 W build_attn_paged_or_null: layer 30 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.921 W build_attn_paged_or_null: layer 31 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.950 W build_attn_paged_or_null: layer 32 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.968 W build_attn_paged_or_null: layer 33 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.017.997 W build_attn_paged_or_null: layer 34 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.018.028 W build_attn_paged_or_null: layer 35 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.018.059 W build_attn_paged_or_null: layer 36 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.018.080 W build_attn_paged_or_null: layer 37 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.018.109 W build_attn_paged_or_null: layer 38 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.018.127 W build_attn_paged_or_null: layer 39 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.018.164 W build_attn_paged_or_null: layer 40 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.018.186 W build_attn_paged_or_null: layer 41 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.018.210 W build_attn_paged_or_null: layer 42 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.020.172 W build_attn_paged_or_null: layer 0 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.020.188 W build_attn_paged_or_null: layer 1 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.020.212 W build_attn_paged_or_null: layer 2 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.020.233 W build_attn_paged_or_null: layer 3 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.020.257 W build_attn_paged_or_null: layer 4 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.020.278 W build_attn_paged_or_null: layer 5 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.020.302 W build_attn_paged_or_null: layer 6 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.020.323 W build_attn_paged_or_null: layer 7 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.020.347 W build_attn_paged_or_null: layer 8 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.020.367 W build_attn_paged_or_null: layer 9 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.020.391 W build_attn_paged_or_null: layer 10 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.020.412 W build_attn_paged_or_null: layer 11 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)
0.05.020.436 W build_attn_paged_or_null: layer 12 took the STATIC path -- no paged context. mctx=0x105a613f0 (compare against DS4P-SET: same pointer = the wrapper set it and the consumer still sees null; different = the graph holds a stale context)

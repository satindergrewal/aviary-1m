# P2-8 finale gate trail, 2026-08-04 (tips through ds4-ports f2a515df)
#
# The queue-not-reject arm (6 concurrent on np2) surfaced a TWO-LAYER defect:
# 1. seq_rm ordering (76da2261): completion-time clear ran before scheduler update
#    re-wrote positions -- stale state for reused seq ids. Fixed; storm SURVIVED.
# 2. THE REAL INJECTION (f2a515df): the scheduler's own id lifecycle -- a stopped
#    group's teardown waited one step; the server relaunched on the reused slot id in
#    that window; queue_request silently overwrote id_to_group; the late sweep erased
#    the NEW request's mapping -> orphaned request -> 'positions are decreasing'.
#    Plus the amplifier: a failed llama_decode returned without updating the scheduler,
#    so the identical bad batch respun forever (880K+ error lines measured) and the
#    CUDA VMM pool ballooned to a FULL CARD (94-96 GiB) -- the storm is also a leak.
# Fixes: eager guarded finish() at update-time; idempotent teardown; pointer-checked
# id_to_group erase; server cancels a failing batch instead of respinning it.
#
# == MAC GATE (arm-1 scenario, CPU, qwen3-4b, np2, 6 concurrent): PASS ==
# 6/6 complete at full length; storm lines: 0; decode failures: 0.
#

# == BOX CUDA WALLS (tip f2a515df -> 324827cb, regen green + unharmed throughout) ==
# ARM 1 queue-not-reject: ★ TRUE PASS — 6/6 HTTP 200 at full 96 tokens, arm1_dec = 0
#   (zero 'positions are decreasing' under concurrent seq reuse: the f2a515df race fix
#   holds at the CUDA wall). GATE_RC=0.
# ARM 2 evict/preempt: WEAK-PASS x3 runs (320-tok, 600-tok, dual-starved 40+8) — all
#   completions full-length, zero storm, but swap NEVER engaged... because the pool was
#   NEVER REAL: the auto-fitter ran after the explicit init and re-sized 40 blocks to
#   7719 (proof: 1815 tokens 'fit' a 768-token pool; max block id 7718 = 7719-1; log
#   shows init@40 then fitter@7719). NINTH ship: 324827cb — explicit block flags now
#   disable the fitter. Gate-harness fix on the way here: fire() waited on the server
#   job and hung (0fce0bc).
# TRUE-STARVED arm-2 (first ever, fitter off -- init@40+8 REAL, fitter lines 0):
#   ★ VERDICT: SWAP-LIVELOCK, honest FAIL-AT-BAR. swap ENGAGED for the first time
#   (max swapped=1) then never drained: 5,032,631 livelock-detector lines in ~3 min
#   (5.1 GB log), zero decodes completing, curls stalled, decode-fails 0.
#   MECHANISM (from code + walls): 3 requests each needing ~38 blocks admitted into a
#   40-block pool (admission allocates only n_prompt+1); none can grow; evict() swaps ONE
#   victim toward 8 CPU blocks (too small to hold 38) and the remaining runners still
#   cannot allocate; the swapped one can never swap in; check_livelock DETECTS the state
#   every tick but nobody ACTS on it -- the server ignores the DEADLOCK status and ticks
#   forever. Storm-class log growth on a new axis.
#   THE FIX (named, parked as the next implementation): recompute-preemption -- when
#   swap-out cannot fit CPU blocks, abort the victim to WAITING with blocks freed and
#   n_past=0; logical_seq already holds prompt+generated tokens so chunked prefill can
#   REPLAY it losslessly (the P1-5 tail-replay concept scheduler-side). Plus: the server
#   must ACT on DEADLOCK status (cancel or backoff), never tick-loop on it.
#
# == Q5 QUENCH-ECON: HONEST PARK, the fit math ==
# Needs a second V4 (dsv4-flash UD-Q4_K_XL ~150 GiB across cards) + drafter serve.
# Free beside the green regen: GPU0 21.6 + GPU1 24.3 GiB = 45.9 total, non-contiguous.
# 150 >> 45.9 -> cannot co-tenant; single-card solo also impossible (150 > 97.9).
# Q5 runs ONLY in a full-box window (regen paused at a boundary, per its own queue rules).
# (embargo footer removed 06:00 -- regen GREEN, walls above are the record)

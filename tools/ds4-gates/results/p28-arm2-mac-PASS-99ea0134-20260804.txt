# P2-8 arm2 Mac FULL PASS, tip 99ea0134, 2026-08-04 06:4x
# cmd: llama-server qwen3-4b -c 4096 -np 3 -b/-ub 128 --kv-paged
#      --n-gpu-blocks 40 --n-cpu-blocks 2 --cache-ram 0 -lv 5 ; 3 x 400-tok concurrent, temp 0
#
# The three-fix chain that got here (each found by a wall, each Grok-verified):
#   9669526a stall verdict only at zero candidates (killed the tick-forever livelock)
#   23a157e5 recompute keeps generated tokens (lossless replay via n_prompt=logical_seq.size())
#   99ea0134 admission passes the allocation DELTA (allocate() already counts the prompt;
#            the double-count serialized every request and hid eviction entirely)
#
# == WALL: FULL PASS, machinery genuinely exercised ==
# occupancy: running=3 x187 steps (true concurrency under pressure)
# evictions: 4    recomputes: 8 (lossless-replay path live -- first real exercise)
# swap-outs: 0 (CPU pool 2 blocks: victims recompute instead, by design)
# completions: 3/3 HTTP 200 at FULL 400 tokens
# 'positions are decreasing': 0    terminal deadlock: 0    decode failures: 0
#
# REMAINING: box CUDA re-gate on the same bar (next card window; regen holds) -- that is
# the last wall between P2-8 arm2 and CLOSED. Swap-out path (CPU pool that FITS victims)
# witnessed earlier only as counter=1-and-drain; a dedicated swap-in/out wall can ride
# the CUDA re-gate config (--n-cpu-blocks 128).

# P2-8 swap-out wall, tip 99ea0134, 2026-08-04 06:5x -- FIRST GENUINE EXERCISE = CRASH
# cmd: same 40-GPU-block starve, --n-cpu-blocks 128 (victims FIT -> swap_out fires)
#
# == VERDICT: FAIL -- server ABORTS on the swap path ==
# 2 evictions -> 2 swap-outs -> GGML_ASSERT "block_table_id OOB" at
# llama-paged-scheduler-impl.cpp:406 (calculate_global_slot_index) inside
# update_slots -> server dead, 0/3 responses.
# MECHANISM (from the assert site): a swapped-out group re-enters populate_batch_from
# as a candidate while its block_table no longer covers its n_past positions --
# swap_out moves/clears GPU block ids but the group's scheduling state still presents
# it as computable (suspects: process_swapped_list promotes without a successful
# swap_in; or swap_out leaves n_past/table inconsistent for the slot-index math).
# NOTE: recompute-preemption (the sibling path) is PROVEN GOOD (8 live replays,
# p28-arm2-mac-PASS-99ea0134). Swap-in/out needs the same wall-driven treatment:
# read swap_out/swap_in + process_swapped_list, fix the state contract, re-run THIS wall.
# PASS bar: swapped>0 mid-run AND drained via swap_in, 3/3 full-length, zero OOB/errors.

# == ROUNDS 2-3 (tips 99ea0134 -> c1bbeef8): two mechanisms fixed, two remain ==
# FIXED: (1) mid-sweep eviction filter (candidate evicted after collection rode the batch
# with CPU ids); (2) growth check on swap-in (victim returned with the same too-short
# table that got it evicted: peers 13 blocks vs 12, OOB at pos 192 -- exact numbers).
# ROUND-3 WALL: OOB GONE, swap round-trips live (3 outs/2 ins), 1/3 completes @400.
# REMAINING (next session's first reads):
#   (i) swap-drain starvation: steady state running=1/swapped=2 until the runner ends --
#       swap_in of the pair keeps failing has_free_gpu_blocks while the runner grows;
#       needs either priority swap-in on free or eviction fairness.
#   (ii) teardown abort 'tensor read out of bounds' (ggml-backend.cpp:349) in the swap
#       copy path: gpu/cpu block id spaces cross in do_block_copy offset math (CPU ids
#       are globally numbered >= num_gpu_blocks but the CPU tensor is 0-based) -- the
#       P1-6 tail-copy scar class. AUDIT do_block_copy + swap_in/out offset adjustment.
